# new-vrm-gf

## Documentation

- **[docs/BUILD.md](docs/BUILD.md)** — build and release runbook: EAS profiles,
  the keystore the production profile needs, iOS local builds, and the traps
  that have already cost a Play rejection.
- **[docs/CREDENTIALS.md](docs/CREDENTIALS.md)** — every credential the project
  uses, where it lives and how to rotate it. No secret values: this repo is
  public. Real values go in `SECRETS.local.md` at the repo root, which is
  gitignored.

